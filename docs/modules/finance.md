---
outline: deep
---

# Finance

Random currency identifiers from a built-in list of currencies.

## `currencyCode`

Returns a random ISO-style currency **code** string.

```ts
dunna.finance.currencyCode();
```

## `currencyName`

Returns a random currency **display name** string.

```ts
dunna.finance.currencyName();
```
