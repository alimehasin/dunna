{
  "$schema": "https://unpkg.com/release-it/schema/release-it.json",
  "github": {
    "release": true
  },
  "git": {
    "commitMessage": "v${version}"
  },
  "hooks": {
    "before:init": ["bun run fmt", "bun run lint", "bun test", "bun run build"]
  }
}
